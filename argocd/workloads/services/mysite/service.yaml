apiVersion: v1
kind: Service
metadata:
  name: mysite
  labels:
    app.kubernetes.io/name: mysite
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mysite
  ports:
    - name: http
      port: 80
      targetPort: http
